fastapi==0.115.0
uvicorn[standard]==0.30.6

# Async SQLAlchemy stack — asyncpg is the async Postgres driver.
# sqlalchemy[asyncio] pulls in greenlet which asyncio mode needs.
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3

pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]
python-dotenv==1.0.1

# Auth
python-jose[cryptography]==3.3.0
# passlib 1.7.4 (2020, unmaintained) breaks on bcrypt >= 4.1: its backend
# self-test hashes a >72-byte string, which newer bcrypt raises on instead of
# truncating. Pin bcrypt to the last compatible release so password hashing
# works. Longer term, consider moving off passlib (e.g. `pwdlib`, or calling
# the `bcrypt` package directly) since passlib is no longer maintained.
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# External API calls — AsyncClient so we don't block the event loop
httpx==0.27.2

# AI
anthropic==0.34.2

# Dev / testing
ruff==0.6.9
pytest==8.3.3
pytest-asyncio==0.24.0
